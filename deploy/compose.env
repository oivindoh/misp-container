# Compose-specific overrides.
# In Compose, the worker runs in a separate container (not localhost).
MISP_BASEURL=http://localhost:8080
SMTP_FQDN=localhost
SIMPLEBACKGROUNDJOBS_SUPERVISOR_HOST=worker
