apiVersion: v1
kind: Service
metadata:
  name: metrics
  labels:
    app.kubernetes.io/name: metrics
spec:
  type: ClusterIP
  ipFamilyPolicy: PreferDualStack
  selector:
    app.kubernetes.io/name: metrics
  ports:
    - name: metrics
      port: 9191
      targetPort: 9191
      protocol: TCP
