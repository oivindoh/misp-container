apiVersion: v1
kind: Service
metadata:
  name: worker
  labels:
    app.kubernetes.io/name: worker
spec:
  type: ClusterIP
  ipFamilyPolicy: PreferDualStack
  selector:
    app.kubernetes.io/name: worker
  ports:
    - name: supervisor
      port: 9001
      targetPort: 9001
      protocol: TCP
